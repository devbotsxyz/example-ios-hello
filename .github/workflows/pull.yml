name: Pull Request

on: [pull_request]

jobs:

  Test:
    name: "Build & Test"
    runs-on: "macos-10.15"
    timeout-minutes: 15

    strategy:
      matrix:
        device:
          - "iPhone 11"
          - "iPad Pro (9.7-inch)"

    steps:
      - name: "Checkout Project"
        uses: actions/checkout@v2

      - name: "Select Latest XcodeBeta"
        uses: devbotsxyz/xcode-select@v1

      - name: "Test (Simulator)"
        uses: devbotsxyz/xcode-test@work
        with:
          destination: "platform=iOS Simulator,name=${{ matrix.device }},OS=14.0"
          result-bundle-path: "Test.xcresult"
          result-bundle-name: "Test results for ${{ matrix.device }}"
          constraints: only-testing:HelloTests

      - name: "Create annotations from result bundle"
        uses: devbotsxyz/xcresult-annotate@work
        if: ${{ always() }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          result-bundle-path: /Users/runner/work/example-ios-hello/example-ios-hello/Test.xcresult

      - name: Foo
        run: find ../ -ls
